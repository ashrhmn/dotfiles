interface:
  display_name: "Grill Me"
  short_description: "Resolve a design through rigorous questions"
  default_prompt: "Use $grill-me to question this idea until every important decision is explicit."
policy:
  allow_implicit_invocation: false
