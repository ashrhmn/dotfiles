interface:
  display_name: "Build First Slice"
  short_description: "Build a proven path for later agent work"
  default_prompt: "Use $build-first-slice to build one complete pattern-setting path before delegating the remaining work."
policy:
  allow_implicit_invocation: false
